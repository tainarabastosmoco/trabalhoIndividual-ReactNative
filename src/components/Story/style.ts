import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  list: {
    backgroundColor: "#000",
  },

  storiesList: {
    backgroundColor: "#000",
    alignItems: "center",
  },

  foto: {
    width: 82,
    height: 82,
  },

  container: {
    backgroundColor: "#000",
    marginTop: 10,
    height: 120,
  },

  nomeUsuario: {
    color: "#fff",
  },
});
